{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb-dap",
            "request": "launch",
            "name": "Debug",
            "preLaunchTask": "Build",
            "program": "${workspaceFolder}/build/debug",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}